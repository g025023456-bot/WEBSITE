משימה שבועית אוטומטית (רצה כל יום ראשון בבוקר): הכנת טיוטת הגיליון הבא של הניוזלטר של "הורה ומיוחד", לקראת שיגור ביום שני ב-8:00.

שלבים, לפי הסדר:

1. קרא תחילה את `CLAUDE.md` בשורש המאגר ואת `newsletter/קרא-אותי-צבי.md`. כל הכללים שם מחייבים (מבנה, מיתוג, כללי תוכן, חוקי HTML למייל). כללי התוכן בסעיף 4 הם עילות פסילה.

2. קבע את תאריך יום שני הקרוב. בדוק מול לוח שנה עברי (WebSearch או hebcal.com) שהוא אינו חג, צום, ערב חג או חול המועד. אם הוא נופל על אחד מאלה, קבע את תאריך היעד לפי סעיף 7 ב-CLAUDE.md (יום רביעי הקרוב או יום שני הבא, הראשון שפנוי) וציין זאת בבירור בקובץ הסיכום. אם אינך מצליח לאמת מול לוח שנה עברי, כתוב זאת באזהרה בולטת בראש הסיכום.

3. עבור על הגיליונות המתוארכים שכבר פורסמו ב-`newsletter/` (וכל טיוטה קיימת ב-`newsletter/טיוטות/`) וזהה אילו נושאים כבר כוסו. בחר נושא זכויות אחד חדש, רלוונטי למשפחות של ילדים עם צרכים מיוחדים בקהל חרדי. אל תמציא פרטים ספציפיים (סכומים, סעיפי חוק, תנאים מדויקים) — כתוב ברמה עקרונית וזהירה בלבד, לפי סעיף 4.

4. שכפל את הגיליון המתוארך האחרון שפורסם ובנה עליו את הגיליון החדש: החלף תוכן בלבד, שמור על המבנה, העיצוב והמדורים אחד לאחד. עדכן במאסטהד את מספר הגיליון (הבא במספור) ואת תאריך היעד: `גיליון N · DD.MM.YYYY`.

5. צור בתיקייה `newsletter/טיוטות/` שני קבצים (צור את התיקייה אם אינה קיימת):
   - `YYYY-MM-DD-מייל.html` — גרסת המייל: בלי תגי OG ובלי סרגל "כל הגיליונות", עם placeholder של קישור הסרה: `{{קישור הסרה · רב-מסר}}`.
   - `YYYY-MM-DD-אתר.html` — גרסת האתר: עם תגי OG וסרגל "כל הגיליונות", בלי קישור הסרה.
   (YYYY-MM-DD = תאריך היעד של השיגור.)

6. הרץ על שתי הגרסאות את כל בדיקות סעיף 6 ב-CLAUDE.md: חיפוש `o@ho-me`, קו מפריד ארוך (—), סכומים ואחוזים (חייב לחזור ריק); ספירת `<tr>`/`</tr>` ו-`<td>`/`</td>` שוות; אין `width="600"` קבוע; `bgcolor` על td בכל רקע צבעוני; אין div ריקים; אין פונטים חיצוניים.

7. כתוב קובץ סיכום `newsletter/טיוטות/YYYY-MM-DD-סיכום.md` ובו: הנושא שנבחר ולמה; תוצאת בדיקת לוח השנה העברי; תוצאות כל הולידציות; ורשימת מה שנשאר לצבי לעשות ידנית:
   - לשלוח טיוטת בדיקה לשמעיה (3104358@gmail.com) עם הערת בדיקה בראש.
   - לקבל אישור מפורש מיהודה. בלי אישור, שום דבר לא נשלח.
   - ביום שני ב-8:00: שיגור ברב-מסר (הדבקת גרסת המייל בעורך HTML, בדיקה עצמית קודם).
   - עדכון האתר: `node publish.js newsletter/טיוטות/YYYY-MM-DD-אתר.html` ואז git push.

חוקים קשיחים:
- אסור לגעת ב-`latest.html`, `newsletter-data.js`, `index.html` או בגיליונות שכבר פורסמו. אתה מכין טיוטה בלבד.
- אסור לבצע git commit או git push.
- אסור לשלוח מייל או כל דבר אחר החוצה.
- אם כבר קיימת טיוטה לתאריך היעד, אל תיצור חדשה — בדוק אותה מחדש (שלב 6) ועדכן את קובץ הסיכום בלבד.
