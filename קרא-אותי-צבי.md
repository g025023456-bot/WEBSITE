# הוראות לצבי — גרסת אתר חדשה (עמותה + מימושים)

**מה זה:** גרסה חדשה של האתר, מפוצלת לשני דפים. עותק נפרד — **האתר החי לא נגעו בו.**
**לא לפרוס בלי אישור יהודה.**

---

## מבנה (HTML סטטי, RTL, ללא build)
- `index.html` — אתר **העמותה** (קהילה, פעילויות, תרומות, סיפור, ניוזלטר). מייל: **a@ho-me.org.il**
- `mimushim.html` — אתר **החברה** (מימוש זכויות, שירותים, בודק זכאות, המלצות). מייל: **o@ho-me.org.il**
- מחשבונים: `router.html` → `app.html` / `bogrim-app.html` / `siud-app.html` (משותפים)
- נכסים: `logo-*.webp`, `gallery-*.webp`, `yanki-*.webp`, `video-noflesh.mp4`, `gallery-data.js`
- `robots.txt` · `sitemap.xml` (כולל כבר את mimushim.html) · `vercel.json` (כותרות אבטחה)

## פריסה (Vercel)
1. לפרוס את **כל התיקייה** כפי שהיא (אין שלב build).
2. לוודא **www → apex** (redirect 308 ל-`ho-me.org.il`) — שלא יתפצל אינדוקס.
3. `vercel.json` כבר כולל כותרות אבטחה (X-Frame, nosniff, CSP, Referrer/Permissions-Policy) — יחולו אחרי deploy.

## SEO (פירוט מלא: `פלט/הוראות-לצבי-SEO-מימושים.md`)
- **Google Search Console:** לאמת בעלות (TXT ב-DNS) → להגיש `sitemap.xml` → Request Indexing ל-mimushim.html.
- **Google Business Profile** בשם "מימושים — מימוש זכויות" לשני המשרדים.

## ⚠️ פתוח — דורש חיבור לפני go-live
- **טפסים לא משדרים עדיין:** ניוזלטר (`handleNewsletter`) + לידים (`window.LEAD_URL` בתחתית כל דף) = placeholders. לחבר לרב-מסר / CRM. צריך לשמור גם **שם + מייל + תיעוד הסכמת דיוור** (חוק).
- **סרטון נופש** (`video-noflesh.mp4`) מכיל ילדים — לאשר הסכמת צילום לפני שעולה.

## עדכון תמונות בגלריה (גם ללא צבי)
לפתוח `gallery-data.js`, להוסיף שורה: `{ src: 'קובץ.webp', cap: 'כיתוב' },`. אין צורך לגעת ב-HTML.

---
*נבנה ע"י דונה · 16/06/2026 · שאלות → יהודה*
